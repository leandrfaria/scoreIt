export interface Member {
  id: number;
  name: string;
  birthDate: string;
  email: string;
  handle: string;     // @ do usuário (sem o '@' armazenado)
  gender: string;
  bio: string;
  profileImageUrl: string;
}
